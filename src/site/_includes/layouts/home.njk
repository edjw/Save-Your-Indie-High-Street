{% extends './base.njk' %}

{% block content %}

    <section class="intro">
        {{ content | safe }}
    </section>
    <hr>
    <section class="places" id="places">
        <h2>Find businesses near you</h2>
        <section class="places_list">
            {% set initial = "" %}
            {% for place in places %}

                {% set currentInitial = place | first %}
                {% if currentInitial != initial %}
                    <h3 class="initial" id="{{ currentInitial }}">
                        {{ currentInitial }}
                    </h3>
                    {% set initial = currentInitial %}
                {% endif %}
                <ul>
                    <li class="place">
                        <a href="{{ place | slugIt | lower }}" id="{{ place | slug }}">{{ place }}</a>
                    </li>
                </ul>

            {% endfor %}
        </section>
    </section>

{% endblock %}